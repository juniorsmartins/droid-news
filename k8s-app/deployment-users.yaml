apiVersion: apps/v1
kind: Deployment
metadata: 
  name: users
spec: 
  replicas: 1
  selector: 
    matchLabels: 
      app: users
  template: 
    metadata: 
      labels: 
        app: users
    spec: 
      containers: 
        - name: users
          image: 957817/droid-users:latest
          ports:
            - containerPort: 8001
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: configmap-users
                  key: port
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: configmap-users
                  key: db_host
            - name: DB_DATABASE
              valueFrom:
                configMapKeyRef:
                  name: configmap-users
                  key: database
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: secret-users
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: secret-users
                  key: password
            - name: NEWS_URL
              valueFrom:
                configMapKeyRef:
                  name: configmap-users
                  key: news_url


